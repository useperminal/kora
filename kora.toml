[server]
host = "0.0.0.0"
port = 8080

[rpc]
# Uses RPC_URL from environment variable

[signer]
# Handled by signers.toml

[fees]
enabled = false

[validation]
enabled = false
max_allowed_lamports = 100000000
max_signatures = 10
allowed_programs = []
allowed_tokens = []
blocked_accounts = []
